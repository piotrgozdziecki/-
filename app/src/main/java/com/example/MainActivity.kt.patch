--- app/src/main/java/com/example/MainActivity.kt
+++ app/src/main/java/com/example/MainActivity.kt
@@ -165,6 +165,11 @@
           setSupportZoom(false)
           builtInZoomControls = false
           displayZoomControls = false
+        }
+        
+        // Suppress MESA rendernode error in AI Studio Cloud Emulator
+        if (Build.FINGERPRINT.contains("generic") || Build.MODEL.contains("Cuttlefish") || Build.HARDWARE.contains("cutf")) {
+            setLayerType(View.LAYER_TYPE_SOFTWARE, null)
         }
 
         if (repository != null) {
