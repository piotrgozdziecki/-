sed -i 's/lampGrad.addColorStop(0, l.color ? l.color.replace('\''\'')\'', `, ${intensity})`).replace('\''rgb'\'', '\''rgba'\'').replace('\''#'\'', '\''rgba('\''): `rgba(56, 189, 248, ${intensity})`);/let parsedColor = `rgba(56, 189, 248, ${intensity})`; if (l.color \&\& l.color.startsWith('\''#'\'')) { let h = l.color; let r = parseInt(h.slice(1, 3), 16) || 255; let g = parseInt(h.slice(3, 5), 16) || 255; let b = parseInt(h.slice(5, 7), 16) || 255; parsedColor = `rgba(${r}, ${g}, ${b}, ${intensity})`; } else if (l.color) { parsedColor = l.color.replace('\''rgb('\'' , '\''rgba('\'').replace('\''\'')\'', `, ${intensity})`); } lampGrad.addColorStop(0, parsedColor);/g' app/src/main/assets/game.html
